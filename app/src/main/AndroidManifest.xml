<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cataldo.chris.memorychallenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mem Challenge"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.cataldo.chris.memorychallenge.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cataldo.chris.memorychallenge.GameActivity"
            android:label="@string/app_name"
            android:parentActivityName="com.cataldo.chris.memorychallenge.MainActivity" >
        </activity>
    </application>

</manifest>